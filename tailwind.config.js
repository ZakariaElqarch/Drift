// tailwind.config.js

module.exports = {
  content: ["./app/**/*.{js,jsx,ts,tsx}", "./components/**/*.{js,jsx,ts,tsx}"],
  theme: {
    extend: {
      // Customizing theme settings if needed
    },
  },
  plugins: [
    // Add any Tailwind CSS plugins if needed
  ],
};
